* git flow release start v1.0.2
* bump archivo /version
* editar changelog para que tenga la versión que se va a sacar
* en /docs/website ejecutar `yarn run version 1.0.2`
* git add -A
* git flow release finish
* git push && git push --tags
* git checkout master && git push

FIN
